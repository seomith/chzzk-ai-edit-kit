# 스트리머 AI 편집 가이드 (치지직 → 쇼츠/롱폼)

> **누구를 위한 문서인가?**
> 치지직에서 라이브 방송하는 스트리머가, **AI(Claude Code 또는 Codex 데스크톱)** 의 도움을 받아
> 4~10시간짜리 VOD를 **30분~1시간 검수**만으로 쇼츠와 롱폼 영상까지 만드는 워크플로우를 정리한 매뉴얼입니다.
>
> 핵심 컨셉: **"AI가 1차 스크리닝 → 사람은 검수만"**

---

## 0. 한눈에 보는 효과

| 작업 | 기존 (수동) | 본 키트 (AI 반자동) |
|---|---|---|
| 6시간 방송 풀시청 | 6시간 | **0분** (AI가 자막+채팅으로 판단) |
| 하이라이트 후보 추출 | 1~2시간 | **5~15분** (자동) |
| 컷 편집 | 2~3시간 | **자동 컷 → 30분 검수** |
| 자막 입히기 | 1~2시간 | **자동 burn-in** |
| 쇼츠 9:16 변환 | 클립당 10분 | **자동** |
| **합계** | **10~14시간** | **약 1시간** |

> 시간이 줄어드는 만큼 **방송, 콘텐츠 기획, 시청자 소통**에 쓸 수 있습니다.

---

## 1. 전체 파이프라인

```
┌─────────────────────────────────────────────────────────────┐
│                      AI 보조 편집 파이프라인                 │
└─────────────────────────────────────────────────────────────┘

[치지직 VOD URL  또는  OBS 녹화본 .mp4]
     │
     ▼
┌──────────────────┐
│ 1. 다운로드      │  yt-dlp (치지직 케이스만, 채팅은 옵션)
│   chzzk_download │  → vod.mp4 (+ chat.json은 --with-chat 시)
└──────────────────┘
     │  (OBS 케이스는 1번 스킵, source.video 또는 vod.mp4 사용)
     ▼
┌──────────────────┐
│ 2. STT (자막화)  │  faster-whisper, 모델 자동 선택
│   transcribe     │  GPU+VRAM 분기 (5080 → large-v3 / CPU → small)
│                  │  → transcript.json
└──────────────────┘
     │
     ▼
┌──────────────────┐
│ 2.5 사전 치환    │  glossary.json 기반 (인명·신조어·게임 용어)
│   correct_       │  + 신뢰도 낮은 단어 자동 마킹
│   transcript     │  → transcript.corrected.json
└──────────────────┘
     │
     ▼
┌──────────────────────────────────────────────┐
│ 2.6 LLM 문맥 보정 (선택)                     │
│   AI 어시스턴트 — prompts/transcript_         │
│   correction.md 따라 needs_review 만 다듬기   │
└──────────────────────────────────────────────┘
     │
     ▼
┌──────────────────┐
│ 3. 신호 분석     │  음량·발화 + (선택) 채팅. 가중치 자동 재배분
│   analyze_signals│  → signals.json
└──────────────────┘
     │
     ▼
┌──────────────────────────────────────────────┐
│ 4. AI 하이라이트 스크리닝 (Claude Code/Codex)│
│    transcript.corrected + signals + chat 읽고│
│    쇼츠/롱폼 후보 분류 → highlights.json     │
└──────────────────────────────────────────────┘
     │
     ▼
┌──────────────────┐
│ 5. 자동 컷       │  ffmpeg → clips/*.mp4
│   cut_clips      │  EDL 내보내기 옵션 있음
└──────────────────┘
     │
     ▼
┌──────────────────┐
│ 6. 쇼츠 변환     │  9:16 리프레임 + 자막 burn-in
│   make_shorts    │  → shorts/*.mp4 (+ 메타데이터 .txt)
└──────────────────┘
     │
     ▼
┌──────────────────┐
│ 7. 사람 검수     │  탐색기에서 mp4 직접 재생 → OK/NG
└──────────────────┘
```

---

## 2. 사전 준비

### 2-1. 필수 프로그램

| 프로그램 | 용도 | 다운로드 |
|---|---|---|
| **Python 3.10+** | 스크립트 실행 | https://python.org |
| **ffmpeg** | 영상 처리 | https://ffmpeg.org (또는 winget install ffmpeg) |
| **Claude Code** *또는* **Codex 데스크톱** | AI 반자동화 진행 | claude.ai/code / openai.com |
| **GPU (선택)** | Whisper 가속 | NVIDIA + CUDA 12 권장 |

#### STT 모델 × 디바이스 시간표 (10시간 방송 기준)

| 모델 | CPU(int8) | GPU(float16) | 한국어 품질 |
|---|---|---|---|
| `tiny` | 12~20분 | 5분 | △ (게임 용어/인명 X) |
| `base` | 24~40분 | 8분 | △ (스트리머 신조어 약함) |
| `small` | 1~2시간 | 15분 | ○ (실용 최소선) |
| `medium` | 3~7시간 | 30~40분 | ◎ (권장) |
| `large-v3` | 10~20시간 | 30분~1시간 | ◎+ |

키트의 `transcribe.py` 는 디바이스/VRAM 자동 감지로 모델을 자동 선택합니다 (부록 A 참조).
사용자가 `--model` 로 명시하면 그 값이 우선.

### 2-2. 키트 설치

```bash
# 1. 키트 폴더 다운로드 후
cd 스트리머_편집키트

# 2. 윈도우 원클릭 설치
install.bat

# (수동 설치는 아래 명령 참고)
# python -m venv venv
# venv\Scripts\activate
# pip install -r requirements.txt
```

### 2-3. AI 도구 연결

이 키트는 **표준 `AGENTS.md`** 형식을 사용합니다.
Codex와 Claude Code 모두 같은 파일을 읽습니다.

**A) Codex 데스크톱 사용자**
1. Codex 데스크톱 설치 후 본인 방송 작업 폴더를 프로젝트로 추가
2. `AGENTS.md`가 자동 로드됨 (이 키트가 제공)

**B) Claude Code 사용자**
1. Claude Code 설치 후 폴더 열기
2. Claude Code도 `AGENTS.md`를 자동 인식
3. 환경에 따라 `CLAUDE.md`만 읽는다면 → 키트의 `AGENTS.md`를 `CLAUDE.md`로
   이름만 복사해도 동일하게 동작 (내용 동일)

> 둘 중 어느 쪽이든 동작 방식은 동일합니다. 이하 가이드에서는 "AI 어시스턴트"로 통칭합니다.

---

## 3. 폴더 구조

방송 한 회차 = 폴더 한 개. 날짜 기준으로 관리합니다.

```
내방송/
├── AGENTS.md                       ← 키트에서 복사 (워크플로우 정의)
├── glossary.json                   ← (선택) 채널 사전, 키트 examples/에 템플릿
├── 260504/                         ← 2026년 5월 4일 방송
│   ├── source.url                  ← 치지직 사용자
│   ├── source.video                ← OBS 사용자 (영상 절대경로 한 줄)
│   ├── vod.mp4                     ← 영상 (다운 결과 또는 직접 복사)
│   ├── chat.json                   ← --with-chat 일 때만
│   ├── transcript.json             ← STT 원본
│   ├── transcript.corrected.json   ← 사전 치환 + LLM 보정본 ★
│   ├── signals.json
│   ├── highlights.json             ← AI 스크리닝 결과 ★ 검수 핵심
│   ├── clips/                      ← 16:9 자동 컷
│   ├── shorts/                     ← 9:16 + 자막 burn-in + .txt 메타
│   └── longform/                   ← 하이라이트 모음
└── 260505/
    └── …
```

> `source.url` 과 `source.video` 는 둘 중 하나만 있으면 됩니다 (둘 다 있어도 source.video 우선).

---

## 4. 사용법 — 한 회차 처리하기

### 4-1. 가장 짧은 방법 (권장)

방송 끝나고:

1. `내방송/260504/` 폴더 만들기
2. **치지직 사용자**: `source.url` 파일에 VOD URL 한 줄 붙여넣기
   **OBS 사용자**: `source.video` 파일에 영상 절대경로 한 줄 (또는 `vod.mp4` 직접 복사)
3. AI 어시스턴트에서 폴더 열고 다음 한 줄:

   > **"260504 방송 처리해줘"**

4. AI가 단계별로 진행 — 자막 교정 단계에서 한 번, 하이라이트 후보 검수에서 한 번 확인 요청
5. 끝나면 `shorts/`, `longform/` 폴더에서 mp4 직접 확인 → 업로드

### 4-2. 채널 사전(glossary.json) 채우기 (한 번만)

본인 채널의 닉네임·게임 용어·신조어를 사전에 등록하면 자막 정확도가 크게 올라갑니다.
키트의 `examples/glossary.example.json` 을 작업 루트(`내방송/glossary.json`)로 복사 후
빈 칸 채우세요. 모든 회차에 자동 적용됩니다.

### 4-3. 단계별 수동 실행 (디버깅·커스터마이징용)

```bash
# 1. VOD 다운로드 (치지직 사용자만, 채팅은 옵션)
python scripts/chzzk_download.py 260504              # 영상만
python scripts/chzzk_download.py 260504 --with-chat  # 영상 + 채팅

# 2. STT (모델 자동 선택)
python scripts/transcribe.py 260504

# 2.5. 사전 치환 + 신뢰도 마킹
python scripts/correct_transcript.py 260504

# 2.6. (선택) AI 어시스턴트에 "260504 자막 LLM 보정해줘"
#      → prompts/transcript_correction.md 따라 진행

# 3. 신호 분석 (채팅 없으면 가중치 자동 재배분)
python scripts/analyze_signals.py 260504

# 4. AI 하이라이트 스크리닝
#    AI 어시스턴트에서: "260504 하이라이트 뽑아줘"
#    → prompts/highlight_screening.md 사용

# 5. 자동 컷
python scripts/cut_clips.py 260504

# 6. 쇼츠 변환 (transcript.corrected.json 자동 우선 사용)
python scripts/make_shorts.py 260504
```

---

## 5. AI 활용 — 자연어 명령 예시

폴더를 AI 어시스턴트에서 열고 다음과 같이 말하면 됩니다.

| 한국어 명령 | AI가 실행하는 것 |
|---|---|
| "260504 방송 처리해줘" | 1~6번 전체 파이프라인 |
| "치지직 URL `xxx` 처리해줘" | 폴더 자동 생성 후 풀 파이프라인 |
| "OBS 녹화본 `D:\xxx.mp4` 로 260504 처리해줘" | source.video 작성 후 다운 단계 스킵 |
| "260504 채팅도 받아서 처리해줘" | `--with-chat` 로 채팅까지 |
| "260504 자막 LLM 보정해줘" | 2.6단계만 (transcript.corrected.json 다듬기) |
| "260504 하이라이트만 다시 뽑아줘" | 4번 (스크리닝)만 재실행 |
| "260504 쇼츠 5개만 더 만들어줘" | highlights.json 보고 추가 후보 추출 |
| "260504 자막 톤을 더 예능스럽게 바꿔줘" | 자막 스타일 프롬프트 변경 |
| "이번 주 방송 다 처리해줘" | 미처리 폴더 일괄 (final 없는 폴더) |
| "이 클립 시작점을 5초 뒤로 밀어줘" | 특정 클립 재컷 |

---

## 6. 검수 가이드 (사람이 하는 부분)

전체 자동화의 마지막 단계는 **사람의 눈**입니다. AI가 만든 `highlights.json`과
실제 mp4를 함께 확인하세요.

### 6-1. 무엇을 봐야 하나

- **시작/끝 타이밍**: 펀치라인이 잘렸거나, 너무 일찍 시작하지 않는가
- **민감 발언**: AI가 못 거른 욕설·저작권 BGM·정치적 발언·개인정보
- **얼굴/캠 위치**: 9:16 리프레임이 캠을 잘 잡았는가
- **자막 오타**: 한국어 STT는 인명·고유명사·신조어에 약함

### 6-2. 빠른 검수 워크플로우

1. `highlights.json` 열어서 한 번 쭉 읽기 (제목·이유만)
2. 의심스러운 것만 mp4 재생
3. NG는 파일명 앞에 `_NG_` 붙이기 (또는 폴더 이동)
4. AI에게 "_NG_ 붙은 거 빼고 업로드 메타데이터 만들어줘"

---

## 7. 출력물 활용

### 7-1. 쇼츠

- 9:16, 자막 burn-in 완료 → 유튜브/인스타/틱톡 바로 업로드
- 권장 길이: 30~60초
- AI가 후보 제목·해시태그도 같이 만들어줌 (`shorts/01_*.txt`)

### 7-2. 롱폼

- 16:9 하이라이트 모음 (15~30분)
- 챕터 마크 자동 생성 (유튜브 설명란에 붙여넣기)
- 썸네일 자동 추출 (지정 시 클립 시작 프레임 N개)

---

## 8. 트러블슈팅

### Q. STT가 너무 느려요
- GPU(NVIDIA) 있으면 `transcribe.py` 가 자동 감지해서 cuda 사용
- 자동 모델 분기로 환경별 최적값 자동 선택 (부록 표 참고)
- 명시적으로 모델 낮추기: `python scripts/transcribe.py 260504 --model small`
- 정 안되면 OpenAI Whisper API(유료, 분당 약 0.006달러)

### Q. 치지직 VOD가 다운로드가 안돼요
- yt-dlp 최신 버전 확인: `pip install -U yt-dlp`
- 그래도 안되면 `chzzk-py` 등 비공식 라이브러리로 폴백 (스크립트 안에 처리)

### Q. AI가 엉뚱한 구간을 하이라이트로 뽑아요
- `prompts/highlight_screening.md` 열어서 본인 채널 톤에 맞게 수정
- 예: "정치 관련 발언은 절대 제외", "ASMR 채널이라 큰 웃음 신호 무시"

### Q. 채팅 로그를 못 가져와요
- 치지직 로그인 쿠키가 필요한 경우가 있음
- `scripts/chzzk_download.py` 안에 쿠키 경로 설정 부분 참고

### Q. 무료 플랜으로 충분한가요?
- Claude Code/Codex 무료 플랜은 일일 한도 있음
- 4~10시간 방송 1회 처리에 보통 30~60회 LLM 호출 발생
- 한도 초과 시 다음날 재개 또는 유료 전환

---

## 9. FAQ

**Q. 치지직 외 다른 플랫폼도 되나요?**
A. 다운로드 단계만 바꾸면 트위치/SOOP/유튜브 라이브 모두 가능. yt-dlp가 대부분 지원합니다.

**Q. 편집 결과를 프리미어/다빈치로 가져갈 수 있나요?**
A. `cut_clips.py`에 `--export-edl` 옵션을 주면 EDL/XML로 내보냅니다. 다빈치 무료에서 import 가능.

**Q. 안전성·저작권은 어떻게 처리되나요?**
A. AI가 자막에서 욕설·BGM 후보를 표시해주지만 **최종 책임은 본인**입니다. 검수 단계에서 반드시 확인하세요.

**Q. 다른 AI 모델 써도 되나요?**
A. 됩니다. `prompts/` 안의 프롬프트는 모델 무관. Claude/GPT/Gemini/로컬 LLM(Ollama+Qwen)에서 모두 사용 가능.

---

## 10. 다음 단계

- [README.md](README.md) — 5분 안에 첫 영상 만들기
- [AGENTS.md](AGENTS.md) — AI 어시스턴트가 읽는 워크플로우 정의 (그대로 복사 사용)
- `scripts/` — 각 단계별 파이썬 코드
- `prompts/` — AI에게 넘기는 프롬프트 템플릿
- `examples/260504_sample/` — 폴더 구조 예시

문제·개선 제안은 본인이 사용하는 톡방·디스코드에 남겨주세요.
