# 용어집 — 키트에서 만나는 단어들

> 스트리머가 키트를 쓰다가 모르는 단어를 만났을 때 AI 어시스턴트에 한 줄:
>
> > **"`OOO` 이 뭐야?"**
>
> 라고 물으면 AI 가 이 파일을 우선 참조해서 한국어로 짧게 답합니다.
> 여기 없으면 일반 지식으로 보충해서 답해줘요.
>
> *AI 한테*: 사용자가 비개발자 스트리머라는 걸 잊지 말고 일상 어휘로,
> 비유 한 줄 + 본질 한 줄로 짧게 답하세요. 길게 강의하지 말 것.

---

## 1. 자주 보는 키트 파일

| 단어 | 한 줄 설명 |
|---|---|
| `AGENTS.md` | AI가 읽는 사용 설명서. "어떤 명령에 어떻게 반응할지" 가 적혀 있어요. (Codex가 1순위로 읽음, Claude Code도 인식) |
| `CLAUDE.md` | Claude Code가 1순위로 읽는 같은 형식의 파일. AGENTS.md 와 내용 같음 |
| `glossary.json` | 본인 채널 사전. 닉네임/게임 용어/신조어를 적어두면 자막이 정확해짐 |
| `glossary.suggested.json` | 키트가 자동으로 모은 사전 후보. 가끔 "글로써리 후보 검토해줘" 로 정식 사전에 머지 |
| `source.url` | 치지직 VOD 주소를 적어두는 텍스트 파일 (한 줄) |
| `source.video` | OBS 등 외부 영상 파일의 절대경로를 적어두는 텍스트 파일 (한 줄) |
| `vod.mp4` | 처리할 영상 본체. 다운로드 결과거나 사용자가 직접 복사해 둔 것 |
| `chat.json` | 치지직 채팅 다시보기 로그 (`--with-chat` 일 때만) |
| `meta.json` | 치지직 VOD의 제목/길이/채널 같은 메타정보 |
| `transcript.json` | STT 자막 원본. 단어 하나하나에 시간이 찍혀 있음 |
| `transcript.corrected.json` | 사전 치환 + LLM 보정 거친 자막. 쇼츠 자막은 이게 우선 사용됨 |
| `signals.json` | 음량 피크/채팅 폭발/발화 밀도로 뽑은 하이라이트 후보 구간 |
| `highlights.json` | AI 가 신호+자막 보고 "이 구간이 진짜 재밌다" 판정한 결과 (★ 검수 핵심) |
| `summary.md` | 한 회차 결과 정리 한 페이지. 만든 쇼츠/안 만든 후보/검수 필요 항목까지 |
| `clips/` | 16:9 원본 비율 자동 컷 결과 |
| `shorts/` | 9:16 세로로 변환 + 자막 박힌 쇼츠 + 메타데이터 .txt |
| `longform/` | 하이라이트 모음(`highlight_full.mp4`) + 유튜브 챕터(`chapters.txt`) |

## 2. 키트 고유 개념

| 단어 | 한 줄 설명 |
|---|---|
| **회차 폴더** | 방송 한 번 = 폴더 한 개. 폴더명은 `YYMMDD` (예: `260504` = 2026-05-04) |
| **작업 루트** | AI 어시스턴트로 연 폴더. 그 아래에 회차 폴더들이 쌓임 |
| **정지 0회 / 논스톱** | 첫 명령부터 결과까지 AI 가 묻지 않고 끝까지 가는 정책 |
| **5개 한도** | 점수 상위 5개 쇼츠만 mp4 로. 나머지는 `summary.md` 목록만 |
| **롱폼 1개** | `include_in_highlight: true` 챕터를 합친 `highlight_full.mp4` 한 개 |
| **warnings 격리** | 욕설·저작권 BGM 의심 구간은 mp4 안 만들고 `summary.md` 표시만 |
| **`_NG_` 접두사** | 검수 후 빼고 싶은 쇼츠 파일명 앞에 붙이는 표시 |
| **needs_review** | 자막에서 신뢰도 낮은 단어가 있어 LLM 보정이 필요한 segment |
| **llm_reviewed** | 그 segment 를 AI 가 한 번 검토했는지 여부 |
| **after** | 사람이 검수해야 할 단계. 키트는 정지 안 함, 결과 보고 사후 검수 |

## 3. AI / ML 용어

| 단어 | 한 줄 설명 |
|---|---|
| **STT** | Speech-to-Text. 말소리를 글자로 받아쓰는 것. 키트는 `faster-whisper` 사용 |
| **LLM** | Large Language Model. ChatGPT/Claude 같은 큰 언어모델 |
| **VAD** | Voice Activity Detection. 음성이 있는 구간만 골라내는 기술. 침묵 구간 자동 스킵 |
| **신뢰도 (prob)** | STT가 한 단어를 얼마나 확신하는지. 0~1 사이, 0.6 미만이면 의심 |
| **토큰** | LLM이 글을 처리할 때 쓰는 작은 조각. 한국어 한 글자가 보통 토큰 1~2개 |
| **청크** | 긴 자료를 나눈 덩어리. 자막 LLM 보정은 30~50 segment 씩 청크로 처리 |
| **프롬프트** | LLM 에 주는 지시문. 키트의 `prompts/` 폴더 안에 다 있음 |
| **빔 서치 (beam_size)** | STT가 여러 후보 중 가장 좋은 답을 고를 때 보는 가짓수. 5 정도면 충분 |

## 4. 영상·음성 기술

| 단어 | 한 줄 설명 |
|---|---|
| **ffmpeg** | 영상·음성을 자르고 합치고 변환하는 만능 도구 (오픈소스). 키트의 컷·쇼츠 변환 모두 이걸로 함 |
| **yt-dlp** | YouTube/치지직/트위치 등에서 영상 다운로드하는 도구 |
| **VOD** | Video On Demand. 다시보기 영상 |
| **9:16** | 세로 비율 (인스타·틱톡·유튜브 쇼츠) |
| **16:9** | 가로 비율 (일반 유튜브·TV) |
| **burn-in 자막** | 자막을 영상에 박아 넣어 끌 수 없게 만든 것. 키트는 **기본 OFF**, `--burn-subs` 명시할 때만 박음 (자막 품질 검증 후 권장) |
| **ass 자막** | 스타일 풍부한 자막 형식 (`.ass`). 키트의 burn-in 용 (큰 흰글씨 + 검정 외곽선) |
| **srt 자막** | 가장 호환성 좋은 자막 형식 (`.srt`). 유튜브 자막 업로드, 프리미어/다빈치 import 모두 가능. 키트는 기본으로 `.srt` 사이드 파일을 영상 옆에 같이 떨어뜨림 |
| **EDL** | Edit Decision List. 컷리스트 한 텍스트 파일. 다빈치/프리미어에 import 가능 |
| **CRF** | x264 압축 품질 숫자. 낮을수록 깨끗·용량 큼. 키트는 20 (고화질·적당 용량) |
| **preset** | x264 인코딩 속도 옵션. `veryfast` 면 빠르게, `slow` 면 작게. 키트는 `veryfast` |
| **AAC** | 음성 코덱. 유튜브 호환 표준 |
| **bitrate** | 1초당 데이터 양. 음성 192k = 192 kbps |
| **mp4 / mkv / mov** | 영상 컨테이너 형식. 키트는 입력에 셋 다 받고 출력은 mp4 |

## 5. 사용 도구

| 단어 | 한 줄 설명 |
|---|---|
| **Codex 데스크톱** | OpenAI의 AI 코딩/작업 데스크톱 앱. 키트의 자연어 명령 처리 |
| **Claude Code** | Anthropic의 같은 류 도구. AGENTS.md/CLAUDE.md 인식 |
| **faster-whisper** | OpenAI Whisper STT 의 빠른 구현. GPU 가속 강함 |
| **auto-editor** | 영상에서 침묵 구간 자동으로 잘라주는 도구 |
| **CUDA / VRAM** | NVIDIA GPU 의 가속 시스템 / GPU 전용 메모리. VRAM 클수록 큰 STT 모델 가능 |

## 6. STT 모델 이름

| 단어 | 의미 |
|---|---|
| `tiny` / `base` / `small` / `medium` / `large-v3` | faster-whisper 모델 크기. 클수록 정확 + 느림. 키트는 GPU/VRAM 보고 자동 선택 |

## 7. 명령행 옵션

| 옵션 | 무엇 |
|---|---|
| `--force` | 결과 파일이 이미 있어도 다시 만들기 |
| `--with-chat` | 치지직 채팅 로그도 받기 (기본 OFF) |
| `--shorts-limit N` | 쇼츠 mp4 만들 개수 (기본 5, 0 = 제한 없음) |
| `--no-suggest` | glossary.suggested.json 자동 누적 끄기 |
| `--burn-subs` | 쇼츠 영상에 자막을 박아 넣기 (기본은 박지 않고 .srt 사이드 파일만) |
| `--no-keep-subs` | .srt 사이드 파일도 만들지 않기 |
| `--no-subs` | 자막 관련 모든 출력 OFF |
| `--export-edl` | 다빈치/프리미어용 EDL 내보내기 |
| `--info-only` | 다운로드 안 하고 메타정보만 조회 |

## 8. 자주 묻는 단어

> 사용자가 일상 대화 톤으로 묻는 표현들을 위한 매핑.

- "스크리닝" / "하이라이트 뽑기" → §3 의 4단계 (AI가 highlights.json 만드는 것)
- "컷팅" / "잘라내기" → 5단계 (ffmpeg로 영상 잘라 mp4 만들기)
- "교정" / "보정" → 자막 사전 치환 (2.5) + LLM 문맥 보정 (2.6)
- "검수" → 사람이 결과 mp4 보고 OK/NG 판단하는 마지막 단계
- "노스톱" / "논스톱" → 정지 0회로 끝까지 자동
- "글로써리" → glossary.json (채널 사전)

---

## AI 응답 가이드 (이 파일 보는 AI 에게)

사용자가 `"X 이 뭐야?"` / `"X 가 뭐지?"` / `"X 설명해줘"` 식으로 물으면:

1. 이 파일에서 X 를 검색
2. 있으면 한국어로 한 줄 + (필요하면) 우리 키트에서 어떻게 쓰이는지 한 줄
3. 없으면 일반 지식으로 답하되, 키트 컨텍스트와 연결지어 끝맺기
4. 무조건 짧게. 일상 어휘. 코드 안 보여줘도 됨

예:
- 사용자: "STT 가 뭐야?"
- AI: "Speech-to-Text — 말소리를 글자로 받아쓰는 것. 우리 키트에선 `faster-whisper` 가 그 역할이고, 결과는 `transcript.json` 에 저장돼요."
